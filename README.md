Welcome to my portfolio.

After pushing into master branch, you need to run the below code to deploy so that the changes will be reflected.

```bash
npm run build
npm run deploy
```

For first time user, you need to install node.js first then run the below:
```bash
npm install
npm start
```